require 'spec_helper'

describe Backer::Memory do
  it 'has a version number' do
    expect(Backer::Memory::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
